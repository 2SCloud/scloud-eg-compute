apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: scloud-observability
  labels:
    app: prometheus
spec:
  selector:
    app: prometheus
  ports:
    - name: http
      port: 9090
      targetPort: http
      protocol: TCP
