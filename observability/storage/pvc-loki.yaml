apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: loki-data
  namespace: scloud-observability
  labels:
    app: loki
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
