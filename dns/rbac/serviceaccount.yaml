apiVersion: v1
kind: ServiceAccount
metadata:
  name: scloud-dns
  namespace: scloud-dns
  labels:
    app: scloud-dns
# Token is mounted — scloud-dns reads ConfigMaps for config hot-reload
automountServiceAccountToken: true
